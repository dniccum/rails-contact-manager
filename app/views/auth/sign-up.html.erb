<% content_for :pageTitle do %>Sign Up <% end %>

<% content_for :body do %>

<div class="home-block main indigo accent-1">
    <div class="container">
        <div class="row">
            <div class="col l12 m12 sm12">
                <h1 class="heading white-text" style="font-weight: 200;">Sign Up</h1>
                <div class="row">
                    <div class="col s12">
                        <div class="card-panel white">
                            <%= form_for(:user, :url => {:action => 'create_contact'}) do |f| %>
                                <div class="row">
                                    <div class="input-field col s6">
                                        <%= f.text_field(:first_name, class: 'validate') %>
                                        <label for="first_name">First Name</label>
                                    </div>
                                    <div class="input-field col s6">
                                        <%= f.text_field(:last_name, class: 'validate') %>
                                        <label for="last_name">Last Name</label>
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="input-field col s12">
                                        <%= f.email_field(:email, class: 'validate') %>
                                        <label for="email">Email</label>
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="input-field col s6">
                                        <%= f.password_field(:password, class: 'validate') %>
                                        <label for="password">Password</label>
                                    </div>
                                    <div class="input-field col s6">
                                        <%= f.password_field(:password_confirmation, class: 'validate') %>
                                        <label for="password">Password</label>
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="col s12">
                                        <button class="btn waves-effect waves-light blue" type="submit" name="action">Submit
                                        </button>
                                    </div>
                                </div>
                            <% end %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<% end %>

<%= render template: "layouts/application" %>
