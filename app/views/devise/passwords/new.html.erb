<% content_for :pageTitle do %>Request Password Reset<% end %>

<% content_for :body do %>

<div class="home-block main indigo accent-1">
    <div class="container">
        <div class="row">
            <div class="col offset-l3 l6 offset-m1 m10 s12">
                <h1 class="heading white-text" style="font-weight: 200;">Request Password Reset</h1>
                <div class="row">
                    <div class="col s12">
                        <div class="card-panel white">
                            <p>To reset your password, enter your email address below.</p>
                            <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
                                <div class="row">
                                    <div class="input-field col s12">
                                        <%= f.email_field :email, class: 'validate' %>
                                        <%= f.label :email %>
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="col s6">
                                        <button class="btn waves-effect waves-light blue" type="submit" name="action">Reset Password
                                        </button>
                                    </div>
                                </div>
                            <% end %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<% end %>

<%= render template: "layouts/application" %>
